<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookssm.mapper.CategoryMapper">

    <resultMap id="categoryMap" type="com.bookssm.entity.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <sql id="categoryColumns">
        id, name, description, sort_order, created_at
    </sql>

    <select id="findById" resultMap="categoryMap">
        SELECT <include refid="categoryColumns"/>
        FROM category
        WHERE id = #{id}
    </select>

    <select id="findByName" resultMap="categoryMap">
        SELECT <include refid="categoryColumns"/>
        FROM category
        WHERE name = #{name}
    </select>

    <select id="findAll" resultMap="categoryMap">
        SELECT <include refid="categoryColumns"/>
        FROM category
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="countBooks" resultType="int">
        SELECT COUNT(1)
        FROM book
        WHERE category_id = #{id}
    </select>

    <insert id="insert" parameterType="com.bookssm.entity.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, description, sort_order)
        VALUES (#{name}, #{description}, #{sortOrder})
    </insert>

    <update id="update" parameterType="com.bookssm.entity.Category">
        UPDATE category
        SET name = #{name},
            description = #{description},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM category
        WHERE id = #{id}
    </delete>
</mapper>
