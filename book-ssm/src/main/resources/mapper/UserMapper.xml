<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookssm.mapper.UserMapper">

    <resultMap id="userMap" type="com.bookssm.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUsername" resultMap="userMap">
        SELECT id, username, password, nickname, phone, email, role, status, created_at, updated_at
        FROM user
        WHERE username = #{username}
    </select>

    <select id="findById" resultMap="userMap">
        SELECT id, username, password, nickname, phone, email, role, status, created_at, updated_at
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.bookssm.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, nickname, phone, email, role, status)
        VALUES (#{username}, #{password}, #{nickname}, #{phone}, #{email}, #{role}, #{status})
    </insert>

</mapper>
